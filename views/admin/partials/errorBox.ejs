<% if(typeof errors != "undefined" && errors.length > 0){%>
    <div class="alert alert-danger">
        <ul class="mb-0">
        <% for(let i = 0; i < errors.length; i++){ %>
            <li><%= errors[i].msg %></li>
        <% } %>
        </ul>
    </div>
<% } %>